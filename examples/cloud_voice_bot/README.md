# Cloud Voice Bot

Voice app skeleton for cloud-backed experiments.

The SDK runtime should handle:

- Wi-Fi connection
- Microphone capture
- WebSocket/MQTT transport
- Cloud response streaming
- Speaker playback

Set cloud credentials outside source code:

```bash
export OPENAI_API_KEY="..."
```

